import { Omit as OOmit } from '../Object/Omit';
import { Tuple } from './Tuple';
/** Transform a **tuple** into an **`object`**
 * @param T to transform
 * @returns **`object`**
 * @example
 * ```ts
 * ```
 */
export declare type ObjectOf<T extends Tuple> = T extends unknown ? OOmit<T, keyof any[]> : never;
