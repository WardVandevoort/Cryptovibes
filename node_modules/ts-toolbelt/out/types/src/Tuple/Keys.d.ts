import { Exclude } from '../Union/Exclude';
import { Tuple } from './Tuple';
/** Get the keys of a **tuple**
 * @param T
 * @returns **`keyof`**
 * @example
 * ```ts
 * ```
 */
export declare type Keys<T extends Tuple> = (Exclude<keyof T, keyof any[]> & keyof T) | number;
